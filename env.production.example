# Production Environment Variables
# Copy this file to .env.production and fill in your values

# Application Settings
NODE_ENV=production
API_PORT=3000
CORS_ORIGIN=https://your-domain.com

# Database Configuration
DB_HOST=postgres
DB_PORT=port
DB_USER=_db_name
DB_PASSWORD=your_secure_password
DB_NAME=temporal

# Temporal Configuration
TEMPORAL_ADDRESS=temporal:7233

# Email Configuration (for reminders)
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=your-email@gmail.com
EMAIL_SMTP_PASS=your-app-password
EMAIL_FROM=your-email@gmail.com
EMAIL_TO=your-email@gmail.com

# Google Gemini API (for cover letter generation)
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-1.5-flash

# Optional: External Database (if not using Docker)
# DB_HOST=your-external-db-host
# DB_PASSWORD=your-external-db-password

# Optional: External Temporal (if not using Docker)
# TEMPORAL_ADDRESS=your-temporal-host:7233
# API
API_PORT=3000
CORS_ORIGIN=http://localhost:3001

# Temporal
TEMPORAL_ADDRESS=localhost:7233

# Email (SMTP)
EMAIL_SMTP_HOST=SENDGRID_HOST
EMAIL_SMTP_PORT=SENDGRID_PORT
EMAIL_SMTP_USER=apikey
EMAIL_SMTP_PASS=SENDGRID_API_KEY
EMAIL_FROM=FROM_EMAIL_SEND_GRRID_REGISTERED
EMAIL_TO=TO_EMAIL

# Gemini
GEMINI_API_KEY=GEMINI_API_KEY
GEMINI_MODEL=gemini-1.5-flash

# Database (PostgreSQL from Temporal Docker)
DB_HOST=localhost
DB_PORT=DB_PORT
DB_USER=DB_USER
DB_PASSWORD=DB_PASSWORD
DB_NAME=DB_NAME